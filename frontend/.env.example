# ================================
# BACKEND API CONFIGURATION
# ================================
# Backend API base URL
# Development: Use your local IP address (not localhost for Expo)
# Example: http://192.168.1.100:5000
# Production: Use your deployed backend URL
EXPO_PUBLIC_BACKEND_URL=http://192.168.1.100:5000

# ================================
# RAZORPAY PAYMENT GATEWAY
# ================================
# Razorpay Key ID (same as backend)
# Get from: https://dashboard.razorpay.com/app/keys
EXPO_PUBLIC_RAZORPAY_KEY_ID=rzp_test_xxxxxxxxxxxx

# ================================
# NOTES
# ================================
# 1. Never commit the actual .env file to version control
# 2. Keep .env in .gitignore
# 3. For Expo, all environment variables must start with EXPO_PUBLIC_
# 4. After changing .env, restart the Expo development server
# 5. For production builds, set these in your build service (EAS Build, etc.)
#
# FINDING YOUR LOCAL IP ADDRESS:
# Windows: Run 'ipconfig' in Command Prompt, look for IPv4 Address
# Mac/Linux: Run 'ifconfig' in Terminal, look for inet address
# Make sure your phone and computer are on the same WiFi network
