import VerificationQueueSection from './VerificationQueueSection';

export default function DeliveryVerificationSection() {
  return (
    <VerificationQueueSection
      role="delivery"
      title="Delivery verification"
      description="Review delivery partner identity documents and approve or reject accounts."
    />
  );
}
